<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tinet.pushtest.mapper.ChatMetricAgentMapper">

    <insert id="insertOrUpdate">
        INSERT INTO chat_metric_agent
        <trim prefix="(" suffix=")" suffixOverrides=",">
            enterprise_id, cno,
            <if test="today_completed_sessions != null">today_completed_sessions,</if>
            <if test="real_time_bot_sessions != null">real_time_bot_sessions,</if>
            <if test="real_time_locked_sessions != null">real_time_locked_sessions,</if>
            <if test="real_time_reception_sessions != null">real_time_reception_sessions,</if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            #{enterprise_id}, #{cno},
            <if test="today_completed_sessions != null">#{today_completed_sessions},</if>
            <if test="real_time_bot_sessions != null">#{real_time_bot_sessions},</if>
            <if test="real_time_locked_sessions != null">#{real_time_locked_sessions},</if>
            <if test="real_time_reception_sessions != null">#{real_time_reception_sessions},</if>
        </trim>
        ON CONFLICT (enterprise_id, cno) DO UPDATE
        <trim prefix="SET" suffixOverrides=",">
            <if test="today_completed_sessions != null">
                today_completed_sessions = chat_metric_agent.today_completed_sessions + #{today_completed_sessions},
            </if>
            <if test="real_time_bot_sessions != null">
                real_time_bot_sessions = chat_metric_agent.real_time_bot_sessions + #{real_time_bot_sessions},
            </if>
            <if test="real_time_locked_sessions != null">
                real_time_locked_sessions = chat_metric_agent.real_time_locked_sessions + #{real_time_locked_sessions},
            </if>
            <if test="real_time_reception_sessions != null">
                real_time_reception_sessions = chat_metric_agent.real_time_reception_sessions + #{real_time_reception_sessions}
            </if>
        </trim>
    </insert>
</mapper> 