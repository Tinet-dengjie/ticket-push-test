<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tinet.pushtest.mapper.ChatMetricAppMapper">

    <insert id="insertOrUpdate">
        INSERT INTO chat_metric_app
        <trim prefix="(" suffix=")" suffixOverrides=",">
            enterprise_id, app_id,
            <if test="today_visitor_sessions != null">today_visitor_sessions,</if>
            <if test="today_completed_sessions != null">today_completed_sessions,</if>
            <if test="real_time_sessions != null">real_time_sessions,</if>
            <if test="real_time_reception_sessions != null">real_time_reception_sessions,</if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            #{enterprise_id}, #{app_id},
            <if test="today_visitor_sessions != null">#{today_visitor_sessions},</if>
            <if test="today_completed_sessions != null">#{today_completed_sessions},</if>
            <if test="real_time_sessions != null">#{real_time_sessions},</if>
            <if test="real_time_reception_sessions != null">#{real_time_reception_sessions},</if>
        </trim>
        ON CONFLICT (enterprise_id, app_id) DO UPDATE
        <trim prefix="SET" suffixOverrides=",">
            <if test="today_visitor_sessions != null">
                today_visitor_sessions = chat_metric_app.today_visitor_sessions + #{today_visitor_sessions},
            </if>
            <if test="today_completed_sessions != null">
                today_completed_sessions = chat_metric_app.today_completed_sessions + #{today_completed_sessions},
            </if>
            <if test="real_time_sessions != null">
                real_time_sessions = chat_metric_app.real_time_sessions + #{real_time_sessions},
            </if>
            <if test="real_time_reception_sessions != null">
                real_time_reception_sessions = chat_metric_app.real_time_reception_sessions + #{real_time_reception_sessions}
            </if>
        </trim>
    </insert>
</mapper> 